{
  "name": "cloudflare-r2-dev-server",
  "scripts": {
    "start": "bun run src/index.ts",
    "proxy": "local-ssl-proxy --key ../../localhost+1-key.pem --cert ../../fullchain.pem --source 5454 --target 4646"
  },
  "dependencies": {
    "hono": "^4.5.10"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
